[package]
name = "ope-protocol"
description = "OPE Confidential AI wire protocol types and NDJSON stream codec (gateway ↔ engine ↔ client)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "../../README.md"
rust-version.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
